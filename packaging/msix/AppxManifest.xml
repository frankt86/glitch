<?xml version="1.0" encoding="utf-8"?>
<!--
  Publisher must match the code-signing certificate's Subject exactly.
  Dev builds use CN=GlitchDev (self-signed, generated by build-msix.ps1).
  Store submissions require a real cert whose Subject is your Partner Center
  publisher display name.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity
    Name="GlitchApp"
    Publisher="CN=GlitchDev"
    Version="__VERSION__"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>Glitch</DisplayName>
    <PublisherDisplayName>Glitch</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <!--
      Windows 11 (10.0.22000.0) guarantees the Evergreen WebView2 runtime is
      pre-installed. Dioxus desktop requires WebView2 — targeting Win 11+
      avoids a separate runtime bootstrapper.
    -->
    <TargetDeviceFamily
      Name="Windows.Desktop"
      MinVersion="10.0.22000.0"
      MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application
      Id="Glitch"
      Executable="glitch.exe"
      EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="Glitch"
        Description="AI-native markdown knowledge base"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png"
        BackgroundColor="#0f1117">
        <uap:DefaultTile
          Wide310x150Logo="Assets\Wide310x150Logo.png"
          ShortName="Glitch" />
        <uap:SplashScreen
          Image="Assets\SplashScreen.png"
          BackgroundColor="#0f1117" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <!-- Required: Glitch spawns child processes (claude CLI, git) and writes
         to arbitrary vault paths chosen by the user. -->
    <rescap:Capability Name="runFullTrust" />
    <!-- Required: article extractor fetches URLs -->
    <Capability Name="internetClient" />
    <!-- Required: named-pipe IPC for MCP permission server -->
    <Capability Name="privateNetworkClientServer" />
  </Capabilities>

</Package>
