name: build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  release:
    types: [created]

env:
  CARGO_TERM_COLOR: always

jobs:
  # ── Test ──────────────────────────────────────────────────────────────────
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Run tests
        run: cargo test --workspace

  # ── Release build + installers ───────────────────────────────────────────
  build:
    name: Build (Windows x64)
    runs-on: windows-latest
    needs: test
    permissions:
      contents: write   # required to upload release assets

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Build release binary
        run: cargo build --release -p glitch

      - name: Build Inno Setup installer (GlitchSetup.exe)
        shell: pwsh
        run: |
          winget install --id JRSoftware.InnoSetup --silent --accept-source-agreements --accept-package-agreements
          $iscc = "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe"
          New-Item build -ItemType Directory -Force | Out-Null
          & $iscc packaging\inno\glitch.iss

      - name: Package MSIX
        shell: pwsh
        env:
          MSIX_CERT_PFX: ${{ secrets.MSIX_CERT_PFX }}
          MSIX_CERT_PASSWORD: ${{ secrets.MSIX_CERT_PASSWORD }}
        run: .\packaging\msix\build-msix.ps1 -SkipBuild

      # Upload all distributable artifacts from build/.
      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: GlitchSetup
          path: build/GlitchSetup.exe
          if-no-files-found: error

      - name: Upload MSIX
        uses: actions/upload-artifact@v4
        with:
          name: glitch-msix
          path: build/glitch.msix
          if-no-files-found: error

      - name: Upload raw binary
        uses: actions/upload-artifact@v4
        with:
          name: glitch-exe
          path: target/release/glitch.exe
          if-no-files-found: error

      # Attach all three to GitHub Releases.
      - name: Attach to GitHub Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/GlitchSetup.exe
            build/glitch.msix
            target/release/glitch.exe
